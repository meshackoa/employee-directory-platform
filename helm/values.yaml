# Backend configuration
backend:
  image: 381155823715.dkr.ecr.us-east-1.amazonaws.com/employee-backend
  tag: be-development-20260723-020620
  replicas: 1
  port: 5000

# Frontend configuration
frontend:
  image: 381155823715.dkr.ecr.us-east-1.amazonaws.com/employee-frontend
  tag: fe-development-20260723-020620
  replicas: 1
  port: 80

# Service Account for S3 + Secrets Manager access
serviceAccount:
  name: app-sa
  roleArn: "arn:aws:iam::381155823715:role/employee-platform-cluster-dev-app-sa"

# External Secrets - AWS Secrets Manager
externalSecrets:
  secretName: "employee-platform-cluster-dev/employee-app"
  refreshInterval: "1m"

# Ingress via ALB
ingress:
  enabled: true
  ingressClass: alb

# S3 bucket
s3:
  bucket: "employee-platform-bucket-dev"
  region: "us-east-1"

# CloudWatch Logging
logging:
  logGroup: "/employee-platform/app"
  environment: "dev"

# Monitoring - kube-prometheus-stack
monitoring:
  enabled: false

kube-prometheus-stack:
  alertmanager:
    enabled: false
  grafana:
    adminPassword: admin123
    service:
      type: LoadBalancer
      annotations:
        service.beta.kubernetes.io/aws-load-balancer-scheme: internet-facing
        service.beta.kubernetes.io/aws-load-balancer-nlb-target-type: ip
    sidecar:
      dashboards:
        enabled: true
        label: grafana_dashboard
        labelValue: "1"
        searchNamespace: ALL
    additionalDataSources:
      - name: CloudWatch
        type: cloudwatch
        uid: cloudwatch
        access: proxy
        jsonData:
          authType: default
          defaultRegion: us-east-1
          logGroups:
            - arn: ""
              name: /employee-platform/app
    serviceAccount:
      annotations:
        eks.amazonaws.com/role-arn: "arn:aws:iam::381155823715:role/employee-platform-cluster-dev-app-sa"
  prometheus:
    prometheusSpec:
      serviceMonitorSelectorNilUsesHelmValues: false
      podMonitorSelectorNilUsesHelmValues: false
      retention: 7d
      storageSpec:
        volumeClaimTemplate:
          spec:
            storageClassName: gp2
            accessModes: ["ReadWriteOnce"]
            resources:
              requests:
                storage: 10Gi
  nodeExporter:
    enabled: true
  kubeStateMetrics:
    enabled: true
